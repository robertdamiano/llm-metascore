{
  "name": "llm-metascore-functions",
  "main": "dist/index.js",
  "scripts": {
    "build": "node build.js",
    "serve": "npm run build && firebase emulators:start --only functions",
    "deploy": "firebase deploy --only functions"
  },
  "engines": {
    "node": "18"
  },
  "dependencies": {
    "cheerio": "^1.0.0",
    "firebase-admin": "^11.11.0",
    "firebase-functions": "^4.5.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "esbuild": "^0.19.12",
    "typescript": "^5.0.0"
  }
}
